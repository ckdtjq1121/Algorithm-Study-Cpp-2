
#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(0);

    int testCase;
    cin >> testCase;

    while (testCase--)
    {
        int k;
        cin >> k;

        // 연속하는 장을 합칠 수 있다.
       
        vector<int> a(k);

        for (int i = 0; i < k; i++)
        cin >> a[i];
    }
}
